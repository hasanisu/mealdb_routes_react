import React from 'react';
import { Button } from 'react-bootstrap';

const MealDetail = () => {
    return (
        <div>
            <h2>Single Meal Detail page</h2>
            <Button>Get View</Button>
        </div>
    );
};

export default MealDetail;